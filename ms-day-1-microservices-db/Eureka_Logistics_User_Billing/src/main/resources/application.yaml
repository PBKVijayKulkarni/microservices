server:
  port: 8761

spring:
  application:
    name: Eureka_Logistics_User_Billing
  cloud:
    service-registry:
      auto-registration:
        enabled: false   

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false